mixin annotation(iconUrl, text)
    div.annotation
        include __icon/annotation__icon.pug
        include __text/annotation__text.pug
        +annotation__icon(iconUrl)
        +annotation__text(text)
        